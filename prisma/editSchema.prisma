model Log {
  id               String   @id
  sessionID        String   @default(uuid())
  displayTimeStamp String   @default("")
  timeStamp        BigInt
  title            String
  tags             String[]
  message          String?
}
